import { ProfileView } from "@/features/worker/profile/profile-view";

export const metadata = {
  title: "My Profile - Worker Portal | KaushalyaSetu",
  description: "Verified cooperative worker profile, skills, and institutional credentials.",
};

export default function WorkerProfilePage() {
  return <ProfileView />;
}
